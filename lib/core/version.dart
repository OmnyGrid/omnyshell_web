/// This web client's version, surfaced in the UI (e.g. the login footer).
///
/// Keep in sync with `version:` in `pubspec.yaml`. The OmnyShell package version
/// is not mirrored here — read `omnyShellVersion` from
/// `package:omnyshell/omnyshell_client_web.dart` instead.
library;

/// This web client's version (matches `pubspec.yaml`).
const String webClientVersion = '1.14.4';
